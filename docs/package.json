{
  "name": "byonk-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "npm run generate-api && rspress build",
    "preview": "rspress preview",
    "generate-api": "npx tsx scripts/generate-api-docs.ts"
  },
  "devDependencies": {
    "rspress": "^1.40.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
